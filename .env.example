# TLC Port Registry
# All port assignments for Docker and local development in one place.
# Docker-compose reads this automatically. Local dev scripts reference .tlc.json.
#
# Port Range: 3140-3159 (TLC reserved block)
#
# Usage: docker-compose -f docker-compose.dev.yml up

# --- Docker Services ---
DASHBOARD_PORT=3147
DB_PORT=5433
APP_PORT=5001
DBADMIN_PORT=8081
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

# --- Local Development (not used by Docker) ---
# TLC_PORT=3148        # Local TLC server (when running outside Docker)
# VITE_DEV_PORT=4000   # Vite dev server (dashboard-web)
